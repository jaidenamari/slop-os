I want to create a way for agents to log their tasks into a sort of structured session id that can easily be referenced to provide context to another agent. 

ie. 

operation: read, write, prompt, and file_path, or prompt path/call, tool_input

